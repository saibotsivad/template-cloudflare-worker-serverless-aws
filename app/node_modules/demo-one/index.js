/*

You need to use the `addEventListener` as the insertion point for
Cloudflare Workers. This makes it hard to test anything in this
code, so this file is mostly just a wrapper file, and it shouldn't
have logic in it that you would write unit tests for.

*/

import { handleRequest } from './handle-request.js'

addEventListener('fetch', event => event.respondWith(handleRequest(event.request)))
